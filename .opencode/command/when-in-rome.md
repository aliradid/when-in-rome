---
{"description": "Write commits, branches and PRs in this repo's own house style for the rest of the session"}
---

Use the `when-in-rome` skill for the rest of this session: before any git commit,
branch name, or pull request, read the repository's history and match its house style
(case, length, prefixes, mood, bodies, branch naming, PR shape). Never add AI
attribution lines, never narrate, never use a stock template. These rules persist
until I say "stop when-in-rome".
