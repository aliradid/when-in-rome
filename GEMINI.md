# when-in-rome

When in Rome, commit as the Romans do. Follow the rules imported below in full: read the repository's git history before any commit, branch or PR, and write in that house style. Never add AI attribution lines; never narrate; never use a stock template.

@./skills/when-in-rome/SKILL.md
